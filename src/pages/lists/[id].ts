import type { NextPage } from "next";

const ListPage: NextPage = () => {
  const data = console.log("data");
  return (
    <div>
      <h1>Hi in list page</h1>
    </div>
  );
};

export default ListPage;
